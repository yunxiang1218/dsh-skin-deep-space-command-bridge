# 更新记录

本项目遵循语义化版本；日期为本地时间。

## 0.4.0 — 纤细驾驶台与玻璃工作屏（2026-09-18）

- 外景默认接入四张 4000 像素宽的天文原片，缩小多余的超出屏幕区域并移除额外缩放；飞行控制新增「高清实景 / 艺术全景」选择，解码完成后统一切换三窗并保存偏好。
- 巡航改为随机二维路径，上下与左右具有相同幅度范围，转向时速度和加速度连续；星点、跃迁光束和背景共用偏移，整舱手动相机保持独立。
- 高清照片由 Worker 解码、逐条上传 GPU，再进行场景混合，减少换景时整张图片上传造成的停顿；GPU 不可用时继续使用已解码的 CSS 背景。
- 修复影像模式快速切换时旧任务覆盖新选择、取消后仍保留纹理的问题；上传在页面可见且连续 10 秒没有进展时回退，隐藏页面暂停不计入超时。
- 根据用户提供的六张驾驶台造型参考重做视觉方向：纤细银色轮廓、通透主窗、简洁弧形台面和克制的冷白灯光，移除叠加梁、成排螺栓与厚重黑色装甲框。
- 新增 `cockpit-frame-v3.png` 和 `cabin-titanium-v3.png`，分别保留内置 Image Gen 实际输出的 1672×941 和 1254×1254 像素；没有改走 API，也没有放大后宣称原生 4K。
- 按新外壳重新校准舷窗遮罩和三屏四角投影，缩减屏内装饰空间，采用细玻璃边缘；继续使用原生 DSH 节点及既有悬浮、拖动、嵌回和飞行控制逻辑。
- 保存完整生成提示词、实际尺寸、素材哈希与设计说明，保留旧外壳、舱壁材质、星景来源和 0.3.0 验证记录。新版测试结果须对应新版运行报告，旧版高刷新率数据不自动延用。详见 `docs/cockpit-redesign-2026-09-17.md`。

## 0.3.0 — 可拖动工作屏与随机星域跃迁

- 三个悬浮屏共享宽度预算，默认不再相互遮挡；标题栏支持鼠标拖动、键盘微调、边缘限制和再次悬浮时恢复位置，保留原生编辑器与草稿。
- 顶部和底部装饰文字合并为一个小型 `yunxiang` 角部署名，来源说明在同一处展开。
- 更新驾驶舱金属材质，并按真实图片边界重画三扇舷窗遮罩；驾驶台拼接段也使用遮罩，修复侧窗下角黑块。
- 新增青金云海、紫蔷薇星系、冰蓝尘海、赤金星炉四种带完整来源记录的艺术合成星景。
- 极快航行超过五秒后随机前往其余星域，先解码、再平滑渐变，随后自动返回快速；支持隐藏暂停和减少动态效果。
- 速度按钮改为连续滑条，保留三个档位区间；距离滑条改为舷窗/转头按钮滚轮操作，缩小转头按钮，保护聊天与工作屏内部滚动。
- 去除旧 30 fps 软件上限，拖动按 rAF 合并；船舱与屏幕直接更新变换，避免重复节点发现、纹理尺寸写入和每次指针事件同步存储。
- WebGL2 批量绘制星点与跃迁射线；Canvas 回退、GPU context loss/recovery 验证齐备。实际显示帧率仍需在对应设备前台实测。

## 0.2.1 — 安装包瘦身

- 离线安装包（`npm pack` 与 Release 附件）不再包含素材源文件：M83、Westerlund 2、Webb 原始观测 JPEG 与合成中间图，约 23MB。运行时不读取这些文件——CSS 与舷窗、舱体素材在构建阶段已内联进 `lib/client.js`，安装包只保留运行时产物、预览图与署名文档。
- 安装包体积由 44.4MB 降至约 18MB，安装与分发更快；主题功能与渲染结果不受影响（`lib/client.js` 字节未变）。
- 需要在本地替换宇宙背景或查看原始观测照片时，请克隆仓库，流程见 `assets/resource/README.md`。

## 0.2.0 — 首次公开发布

### 舰桥

- 无座椅整舱视角：整舱偏航/俯仰各 ±60°，观察距离 0.7–1.6 同时改变人到驾驶台与舷窗的远近，舱体、舷窗与嵌入屏幕共用同一物理相机。
- 三块透视嵌合工作屏（任务屏、主控制台、AI 核心屏），每块可独立“悬浮/嵌回”，切换不重建原生编辑器并保留草稿。
- 飞行控制：缓慢 / 快速 / 极快三档与停泊，帧率无关的指数缓动；页面隐藏或系统“减少动态效果”时暂停绘制。
- 高舷窗观景与返回驾驶台；转头及远景暴露区域由金属侧壁、顶棚与地板覆盖。
- MISSION PANEL 与 AI CORE PANEL 保留原生节点，补充任务摘要、Agent 工具日志、模型与 Thinking 切换、Token 与上下文压力。
- 由 M83、Westerlund 2 与 Webb“宇宙悬崖”观测照片合成的连续艺术星景，经三个窗口显示同一场景；不含行星层。

### 配色修复

- 此前只覆盖了 `--dsw-alias-markdown-code-block`，而行内 `code` 使用的 `--dsw-alias-markdown-inline-code` 等 token 未覆盖，会回落到官方亮色取值（近白），配上座舱的浅色文字即形成“白底浅字”无法阅读。
- 现在显式覆盖官方主题中全部按明暗取值不同的 token（29 项）、主题工作室可覆盖的全部颜色 token（77 项），以及思考区渐变蒙版、下拉菜单底色、主按钮填充等，共 82 项，且明暗两态取值一致，外观不再随系统主题变化。
- 行内 code 底色 `#22384a`、文字 `#e5edf1`，对比度约 9:1；代码块、引用、标签、气泡、侧栏悬停/选中、滚动条、告警底色等同类问题一并修正。

### 工程

- 42 项单元测试（相机边界、屏幕投影四角、帧率无关缓动、生命周期与清理）。
- `npm run preview` 独立预览宿主；`npm run test:browser` 在已安装的官方 DSH 宿主上做真实界面检查。
- 新增 GitHub Actions CI：构建、单元测试，并校验提交的 `lib/` 与当前源码构建结果一致。
- 新增公开元数据（repository/homepage/bugs/keywords）、`.gitignore` 收敛（不发布 `node_modules`、`test-results`、打包产物），文档中的本机绝对路径已改为环境变量写法。
